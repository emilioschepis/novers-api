{
  "name": "novers-api",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "build": "webpack",
    "deploy": "npm run build && sam deploy",
    "serve": "npm run build -- --watch & sam local start-api"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.64",
    "@types/webpack": "^4.41.25",
    "ts-loader": "^8.0.11",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.2",
    "webpack": "^5.7.0",
    "webpack-cli": "^4.2.0"
  },
  "dependencies": {
    "axios": "^0.21.0"
  }
}
